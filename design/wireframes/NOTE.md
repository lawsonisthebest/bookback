# Wireframe screenshots

All 8 PNGs were generated on the box at 1440×900 with `?capture=1&page=...` via playwright-core + system Chrome.

If these PNGs are missing from a partial commit, copy from box:
`/workspace/bookback-wireframes/design/wireframes/*.png`
